version: '3'
services:
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "5000:5000"
  backend:
    build:
      context: .
      dockerfile: Backend/Dockerfile
    ports:
      - "3000:3000"
